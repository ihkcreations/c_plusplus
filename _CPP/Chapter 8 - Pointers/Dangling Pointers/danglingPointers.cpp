#include <iostream>

int main()
{
    // 3 types of dangling pointers
    // Uninitialized pointer
    // deleted pointer
    // multiple pointer pointing to same memory

    // int *p; --> dangling uninitialized pointer
    //-------------------------------------------------//
}